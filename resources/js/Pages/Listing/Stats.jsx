import PageContainer from '@/Layouts/PageContainer'

export default function Stats ({ className, children, title }) {
  return <PageContainer className={'container'} title={title}>
    Init. Stats page - nothing to see here, yet.
  </PageContainer>
}