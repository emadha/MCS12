export default function NavBarLinkItem ( { className, children, title, href, icon = null } ) {
  return <div className={ 'bg-red-300 list-none h-max inline-block rounded transition-all' }>

  </div>
}